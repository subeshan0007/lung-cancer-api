# Lung Cancer Detection API — CPU-only dependencies
# Pinned compatible versions to avoid numpy/scipy conflicts

fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Core — MUST be compatible set (match training environment)
numpy>=1.24.0,<2.0
scipy==1.12.0
scikit-learn==1.3.2

# ML
xgboost>=2.0.0
imbalanced-learn>=0.11.0
joblib>=1.3.0

# Deep Learning (CPU-only)
torch>=2.1.0
torchvision>=0.16.0

# Quantum
pennylane>=0.32.0
pennylane-lightning>=0.32.0

# Image processing
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Config
pyyaml>=6.0
